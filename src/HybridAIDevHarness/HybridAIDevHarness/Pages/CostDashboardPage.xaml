<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="HybridAIDevHarness.Pages.CostDashboardPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:LocalAiDemos.Shared.Controls">
    <Grid Padding="24,16,24,16" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Spacing="4">
            <TextBlock Style="{StaticResource TitleTextBlockStyle}" Text="Cost Dashboard" />
            <TextBlock Opacity="0.7" Text="Local vs cloud spend, recorded by every chat call." />
        </StackPanel>

        <controls:CostTickerControl Grid.Row="1" x:Name="Ticker" />

        <Button Grid.Row="2" Content="Reset ticker (Shift+R)" Click="Reset_Click" />

        <Border Grid.Row="3" Padding="12" Background="{ThemeResource LayerFillColorDefaultBrush}" CornerRadius="8">
            <ListView x:Name="EventList" />
        </Border>
    </Grid>
</Page>
